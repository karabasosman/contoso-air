apiVersion: v1
kind: ConfigMap
metadata:
  name: contoso-air-config
  labels:
    app: contoso-air-web
    app.kubernetes.io/name: contoso-air-web
    app.kubernetes.io/component: web
    app.kubernetes.io/part-of: contoso-air
  annotations:
    description: "Configuration for Contoso Air web application"
data:
  # Add application configuration here as needed
  # Example:
  # LOG_LEVEL: "info"
  # SESSION_SECRET: "your-secret-here"